function() {
    "use strict";

    function _log(type, msg ) {
        console.log("[" + type + "] " +  msg);
    }

    function info(msg) {
        _log(LOG, )
    }

    return {
        info();
        error();
        trace();
    };

}();